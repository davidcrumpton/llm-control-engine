version: 1
name: Host Health Check

vars:
  server: localhost
  env: dev

steps:
  - name: disk
    exec: ssh {{server}} df -h

prompt: Analyze the {{env}} host.

model: gemma4:e2b
system: You are a senior Linux sysadmin.

output:
  format: markdown
  save: report.md
